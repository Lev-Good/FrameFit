# FrameFit — יומן עבודה

## 2026-09-14 — אפיון ראשוני והקמת תיעוד

### מטרה
לתרגם את הצורך שנידון בשיחה ("מסגרת עץ מכסה חלקים לא-סימטריים ממסך התצוגה ואני רוצה
שהמחשב יכיר רק באזור הגלוי") לאפיון מוצר שאפשר לממש, ולבדוק איזו טכנולוגיה זמינה לבנייה.

### בוצע
- בדיקת סביבת הפיתוח: **Windows (build 26200)**, **.NET SDK 10.0.202**, Node 24, Python 3.14,
  Git 2.45. תיקיית הפרויקט הייתה ריקה (רק `.freebuff`), **ללא מאגר git**.
- מחקר מול תיעוד היצרנים ומקורות מקצועיים, לקביעת מה אפשרי בכל מסלול:
  * NVIDIA — *Adjust desktop size and position* → לשונית Size → *Enable desktop resizing*:
    הפיצ'ר **מותנה**, מופיע בעיקר בתצוגה המזוהה כ-HDTV (בדרך כלל HDMI/DVI), ומאפשר גם
    הזזה ⇒ לא-סימטרי. במסך מחשב רגיל הוא בדרך כלל **אינו זמין**.
  * AMD *GPU Scaling: Center*, Intel *Center Image*, ו-*No scaling* ברזולוציה קטנה —
    **מרכוז בלבד** ⇒ סימטרי.
  * בקרות מיקום ב-OSD של מסך (*H/V Position*) — **אפורות בכניסה דיגיטלית**, קיימות ב-VGA
    ובטלוויזיות.
  * מסקנה: במסך רגיל אין דרך לחתוך את שולחן העבודה באופן לא-סימטרי בשכבת הדרייבר, ולכן
    הפתרון חייב לפעול בשכבת מערכת ההפעלה/החלונות.
- ניסוח ואיפיון מלא של המוצר, כולל ממשק הניהול המבוקש: ארבעה שוליים לכל צד בנפרד,
  **תצוגה מקדימה בזמן אמת על המסך עצמו**, ו**מצב כיול גרפי** בגרירת קצוות.
- קביעת טכנולוגיה: C# / .NET 10 + WPF, אריזה self-contained.

### קבצים שנכתבו
- `README.md`
- `docs/SPEC.md`
- `docs/ARCHITECTURE.md`
- `docs/DECISIONS.md` (D1–D7)
- `docs/TASKS.md`
- `docs/AGENT_LOG.md` (קובץ זה)
- `docs/CHANGELOG.md`

### בדיקות
- **לא בוצעו בדיקות קוד — לא נכתב קוד.** מה שנבדק בפועל: זמינות כלי הפיתוח בסביבה
  (`.NET SDK 10.0.202`, Node, Python, Git), ומצב הספרייה (ריקה).
- אימות המידע הטכני בוצע מול תיעוד ומקורות מקצועיים, וסוכם ב-SPEC סעיף 4 וב-DECISIONS D1.

### בעיות
- לא ניתן לאמת את התנהגות `SHAppBarMessage` על ארבעה צדדים מתוך מחקר תיעודי בלבד.
  **הדבר דורש ספיק מעשי (S0.1) לפני פיתוח**, והוגדר כסיכון מרכזי עם נפילה חלופית.

### פתרון
- הוגדר שלב 0 (ספיקים) כתנאי התחלה, במקום להניח שהמנגנון עובד.

### סטטוס
הושלם (אפיון בלבד). **טרם נכתב קוד.**

---

## 2026-09-14 — קליטת תשובות המשתמש ועדכון ההיקף (Q1–Q7)

### מטרה
לקלוט את תשובות המשתמש לשאלות הפתוחות של האפיון ולעדכן את כל המסמכים הנגזרים, כדי
שלא להישאר עם אפיון שמשקף הנחות מיושנות.

### בוצע
- קליטת תשובות ל-Q1–Q7 ועיבוד ההשפעה של כל אחת מהן על ההיקף:
  * **Q1** (דפדפן, שקופיות, מצגות, נגן מדיה) ⇒ מנוע החלונות מספק כיסוי מלא; **שלב 3 עולה
    למעמד ליבה** בהכנה. נוסף סעיף חדש באפיון: "סוגי התוכן שיש לכסות" עם ההתנהגות הנדרשת
    מכל סוג, כולל דרישת **היסטרזיס** למניעת לולאת מאבק עם תוכניות שמחזירות את עצמן למסך-מלא.
  * **Q2** (מסך בודד/משני) ⇒ תמיכה בריבוי מסכים וייחוס פרופיל (D7) **עוברים לשלב 1**.
  * **Q3** (הגדרה פתוחה לשינוי) ⇒ **FR-11 (Kiosk) ירד מההיקף**; אין שירות רקע ואין הרשאות.
  * **Q4** (ללא מסך התחברות ומשחקים) ⇒ **FR-13 (מנוע דרייבר) ירד מההיקף**; אין מחקר
    NVAPI/ADL/IGCL.
  * **Q5/Q6** (מאגר ציבורי, ללא חתימה) ⇒ אושרו ההחלטות הקיימות; נוספה **NFR-10** —
    פרסום SHA-256 בכל מהדורה והסבר אזהרת SmartScreen.
  * **Q7** (אין פתרון פיזי) ⇒ בוטל מסלול הנסיגה לחך הפיזי; איכות הכיסוי של שכבה 2 היא
    דרישת הצלחה.
- עדכון כל המסמכים: `SPEC.md` (גרסה 0.2), `DECISIONS.md` (D8 חדשה + הערות "עודכן" ל-D3,
  D5, D6, D7), `TASKS.md` (הסרת משימות, הוספת בדיקות תוכן והיסטרזיס), `CHANGELOG.md`.

### קבצים שהושפעו
- `docs/SPEC.md` — נוספו סעיף 1.1 (היקף), סעיף 12 (סוגי תוכן), NFR-10; FR-11 ו-FR-13 סומנו
  כ"מחוץ להיקף"; סעיף 11 הפך מ"שאלות פתוחות" ל"החלטות שהתקבלו"; קריטריוני הצלחה 7–8.
- `docs/DECISIONS.md` — D8, והערות עדכון ל-D3, D5, D6, D7.
- `docs/TASKS.md` — שלב 3 הוגדר כליבה; נוספו בדיקות תוכן והיסטרזיס; הוסרו Kiosk ומנוע דרייבר.
- `docs/CHANGELOG.md` — עדכון רשומת ה-Unreleased.
- `docs/AGENT_LOG.md` — רשומה זו.

### בדיקות
- נבדקה שלמות הקישורים הפנימיים בכל קבצי התיעוד לאחר העריכה.
- **לא בוצעו בדיקות קוד — לא נכתב קוד.**

### בעיות
- אין. העדכון היה תיעודי בלבד; אין שינוי בקוד כי אין קוד.

### פתרון
- לא נדרש.

### סטטוס
הושלם. ההיקף מצומצם יותר מאשר בגרסה 0.1 של האפיון, וממוקד בשלב 0 (ספיקים) ובשלב 1 (MVP).

---

## 2026-09-14 — מימוש המוצר (שלבים 0–3)

### מטרה
לכתוב את התוכנה שסוכמה באפיון: כלי Windows שמתאים את שולחן העבודה לאזור הגלוי, כולל
שוליים לא-סימטריים ותצוגה מקדימה בזמן אמת.

### בוצע
- הוקמה סביבת העבודה: **Windows**, ‏.NET SDK 10.0.202. הבנייה כולה מקומית וללא תלויות חוץ
  מלבד xUnit (שהיה זמין ב-cache של NuGet).
- נכתבו ארבעה פרויקטים: `FrameFit.Core` (לוגיקה טהורה), `FrameFit.Platform.Windows`
  (כל האינטראפ עם Win32), `FrameFit.App` (ממשק WPF בעברית RTL) ו-`FrameFit.Core.Tests`.
- מיושמים כל ארבעת מנגנוני האכיפה: שכבת כיסוי שחורה, חסימת סמן העכבר, צמצום אזור העבודה
  דרך AppBar, ושומר מסך-מלא עם היסטרזיס.
- נכתב **מצב בדיקה עצמית** (`--self-test`) שמריץ את האינטראפ עם Windows בפועל ומחזיר
  קוד יציאה.

### קבצים שהושפעו
- `FrameFit.slnx`, `src/FrameFit.Core/**`, `src/FrameFit.Platform.Windows/**`,
  `src/FrameFit.App/**`, `tests/FrameFit.Core.Tests/**`
- `docs/TASKS.md`, `docs/CHANGELOG.md`, `docs/DECISIONS.md`, `docs/ARCHITECTURE.md`,
  `README.md`, ורשומה זו

### בדיקות
- **52 בדיקות יחידה עוברות** (`dotnet test`) — גיאומטריה, ולידציה, טביעות אצבע,
  התאמת פרופילים, טעינה/שמירה של הגדרות.
- **10 מתוך 10 בדיקות עצמיות עוברות** (`FrameFit.exe --self-test`):
  גדלי מבני האינטראפ, מיפוי מסכים, חישוב אזור גלוי, **הצבת שכבת הכיסוי** (אומת בקריאה
  חוזרת של מלבני החלונות), **צמצום אזור העבודה** (אומת מול `GetMonitorInfo`),
  חסימת סמן העכבר ושחרורה, רישום מקש החירום, ובניית דוח אבחון.
- **בדיקת עשן לאפליקציה:** הורצה ל-8 שניות עם `--tray`; התהליך נותר חי, הלוג נוצר
  ותועדו בו זיהוי המסך ואי-החלת הגדרה אוטומטית (אין פרופיל תואם) — התנהגות בטוחה כמצופה.

### בעיות
1. **מיפוי המסכים החזיר אפס מסכים בשקט.** הבדיקה העצמית חשפה ש-
   `Marshal.SizeOf<MONITORINFOEX>()` מחזיר 72 במקום 104 — כי למבנה היה חסר
   `CharSet.Unicode`, ולכן המאשרל תקצב את שדה המחרוזת כ-ANSI. התוצאה הייתה כשל שקט
   של `GetMonitorInfoW` עם שגיאה 87 (פרמטר שגוי), בלי שום חריגה.
2. **ה-EDID לא זוהה.** המזהה שמוחזר מ-`EnumDisplayDevices`
   (`MONITOR\AUO208D\{guid}\0000`) אינו המזהה שבו נשמרת הרשומה ברישום
   (`4&28798eee&0&UID8388688`). לאחר התיקון לאיתור לפי מזהה חומרה — ה-EDID נקרא בהצלחה.
3. **בדיקת ה-AppBar סימנה כשל שגוי.** היא דרשה שוויון מדויק עם האזור הגלוי, בעוד
   שאזור העבודה גם מפנה מקום לסרגל המשימות ולכן קטן יותר. המנגנון עצמו עבד.
4. **תהליך ישן תפס את מקש החירום ואת קובץ ההרצה.** בדיקת העשן הורצה עם `--tray`,
   ובמצב הזה התהליך ממשיך לרוץ אחרי שחרור המעטפת — הוא תפס את `Ctrl+Alt+Shift+F12`
   וגרם לבדיקה העצמית לדווח "הצירוף תפוס" (שגיאה 1409), ולבנייה להיכשל על קובץ תפוס.
   התהליך נסגר, והבדיקה חזרה לעבור. **זו התנהגות תקינה של מצב `--tray`**, לא באג —
   אך ראוי לזכור אותה בזמן פיתוח.

### פתרון
1. הוספת `CharSet.Unicode` למבנה, **ובנוסף** הוספת בדיקה קבועה בגודלי כל שמונה מבני
   האינטראפ — כי גודל שגוי של מבנה הוא כשל שקט ואינו זורק חריגה. זו הבדיקה הראשונה
   שרצה עכשיו בכל בדיקה עצמית.
2. איתור ה-EDID על ידי מעבר על רשומות החומרה תחת `Enum\DISPLAY\<מזהה>`.
3. שינוי הבדיקה לדרישה הנכונה: **אזור העבודה חייב להיות בתוך האזור הגלוי**, ולא להיות
   שווה לו. ואם ה-EDID אינו זמין — זיהוי הפרופיל נופל בחזרה לשילוב של שם התקן ורזולוציה.

### ממצא חשוב
**ספיק S0.1 (שכבת ה-AppBar) עבר בהצלחה:** ארבעה סרגלי יישום הצמיחו את אזור העבודה
מ-`1920×1032` ל-`1820×992`, כלומר לתוך האזור הגלוי. המשמעות: מקסם, Snap וסרגל המשימות
מכבדים את השוליים ברמת מערכת ההפעלה, ואין צורך בנפילה החלופית. ברירת המחדל של המנגנון
הופעלה בהתאם.

### מה טרם נבדק (בכוונה, לא סומן כהושלם)
- **שומר מסך-המלא טרם נבדק מול תוכן אמיתי** — לא הורצו דפדפן ב-F11, מצב הצגה של מצגות,
  שקופיות תמונות או נגן מדיה. זה הפער המרכזי שנותר.
- כיול בגרירת קצוות, טיפול בשינויי תצוגה בזמן ריצה, אריזה ופרסום.

### סטטוס
הושלם לשלבים 0, 1 ורובו של 3. שלבים 2, 4 ו-5 חלקיים. **טרם פורסמה מהדורה.**

---

## 2026-09-14 — פרסום המאגר והמהדורה הראשונה

### מטרה
לפרסם את הפרויקט במאגר GitHub ציבורי ולהוציא מהדורה ניתנת להורדה.

### בוצע
- נוספו קבצים שנדרשים למאגר ציבורי: `LICENSE` (MIT), `.gitattributes` (סופי שורה
  אחידים), ו-`.github/workflows/build.yml` — בנייה ובדיקות בכל דחיפה, ופרסום אוטומטי
  של מהדורה בעת תגית `v*`.
- `.gitignore` עודכן כך שתקיית כלי העבודה המקומית לא תיכנס למאגר.
- `git init` (ענף `main`) ורישום ראשון: 52 קבצים.
- המאגר נוצר ופורסם: **https://github.com/Lev-Good/FrameFit** (ציבורי),
  עם תיאור ושמונה נושאים (topics).
- **אריזה:** `dotnet publish -c Release -r win-x64 --self-contained -p:PublishSingleFile=true`
  — התוצר: `FrameFit.exe` (63MB) יחד עם ספריות נייטיב של WPF, סך 61MB דחוס.
- **אימות קובץ ההרצה שפורסם:** הוא הורצה בעצמו עם `--self-test` — **כל 10 הבדיקות עברו**.
  כלומר המהדורה נבדקה ולא רק נבנתה.
- **שחרור מהדורה `v0.1.0`** עם שני נכסים: האריזה וקובץ ה-SHA-256, ורשומת שחרור מפורטת
  שמציינת במפורש מה נבדק ומה טרם נבדק.

### קבצים שהושפעו
- `.gitignore`, `.gitattributes`, `LICENSE`, `.github/workflows/build.yml`
- `README.md` (נוסף סעיף הורדה), `docs/CHANGELOG.md`, `docs/TASKS.md`, רשומה זו

### בדיקות
- בנייה: 0 אזהרות, 0 שגיאות. 52 בדיקות יחידה עוברות.
- 10/10 בדיקות עצמיות עוברות — גם על גרסת ה-Debug וגם על **קובץ ההרצה שפורסם**.
- סכום הבדיקה של האריזה **נבדק מול התוצר שפורסם**, ולא רק חושב מקומית:
  * הבנייה המקומית העלתה אריזה בסכום `46adf172…`.
  * דחיפת התגית `v0.1.0` הפעילה את ה-workflow, שדרס את שני הקבצים בגרסה שבנה בעצמו —
    וסכום הבדיקה התחלף ל-`7aabda18…` (הגודל השתנה ב-375,466 בתים).
  * אומת שה-`.sha256` שפורסם תואם בדיוק ל-`digest` של ה-zip שגיטהאב מדווחת — כלומר
    הצמד קבצים עקבי.
  * **הקובץ שפורסם הורד חזרה מגיטהאב, חולץ והורצה בו הבדיקה העצמית: 10/10 עברו.**
    זו הבדיקה החזקה ביותר — היא מאמתת את מה שמשתמשים מורידים בפועל, ולא רק את הבנייה
    המקומית.

### בעיות
- בהרצת האריזה הראשונה נכנס בטעות גם קובץ רשומת השחרור אל תוך ה-zip. זוהה בבדיקת
  תוכן הארכיון לפני יצירת המהדורה, האריזה נוצרה מחדש, וסכום הבדיקה חושב מחדש.

### פתרון
- לארוז רק מתיקיית ההפצה, ולאמת את תוכן ה-zip לפני שער הפרסום. הפרסום בוצע רק לאחר
  שהתוכן אומת.

### הערה על סדר הפעולות
יצירת המהדורה ידנית ואחריה דחיפת התגית גרמה ל-CI להחליף את הקבצים.
**הסדר הנכון להבא:** לדחוף קודם את התגית ולתת ל-CI לבנות ולהעלות את הקבצים, ואז
לעדכן את רשומת השחרור אם צריך — או ליצור את המהדורה ידנית **בלי** לדחוף תגית
שמפעילה את עבודת הפרסום.

בנוסף: ה-workflow תוקן להסרת קובצי `.pdb` לפני האריזה, כדי שהמהדורה לא תכיל סמלי
ניפוי. **ההערה הזו אינה חלה על `v0.1.0`** — האריזה שלו כבר פורסמה, והיא מכילה שלושה
קובצי `.pdb` ישנים. אין בכך נזק (הם אינם נדרשים להרצה), והמהדורה הבאה תהיה נקייה.

### הערה
לא פורסם קובץ התקנה (MSI/Inno Setup) — המהדורה מופצת כאריזה להרצה ישירה. זו בחירה
מודעת, ומתועדת כמטלה פתוחה בשלב 5.

> **עודכן (בהמשך אותו יום):** ההערה אינה תקפה עוד. המהדורה `v0.1.0` הוחלפה ומכילה מעתה
> **מתקין MSI** וקובץ ניידי בודד — ראו הרשומה הבאה. הרשומה המקורית נשמרת כפי שנכתבה.
>
> **הערה שנייה — לא תוקנה, ולכן אינה חלה:** ה-workflow **עדיין** דורס נכסים באותו שם בעת
> דחיפת תגית (`softprops/action-gh-release` דורס כברירת מחדל). מאחר ששמות הקבצים קבועים
> לפי התגית, דחיפה חוזרת של `v0.1.0` תחליף את קבצי השחרור בגרסה שה-CI בנה — וזה מקובל,
> כי ה-CI הוא מקור האמת. מה שכן תוקן: ה-workflow מלא־מחדש לבנייה ולבניית המתקין, והוא
> **מוסיף** את קובצי ה-`SHA-256` שמייצר בעצמו.

### סטטוס
הושלם. המהדורה זמינה ב-https://github.com/Lev-Good/FrameFit/releases/tag/v0.1.0

---

## 2026-09-14 — מתקין MSI, גרסה ניידי כקובץ יחיד, ובדיקת קבלה

### מטרה
לייצר **קובץ מתקין רגיל**, כך שלא יהיה צורך לשמור תיקייה שלמה שהורדה מגיטהאב, ולאמת
שההתקנה עצמה עובדת — לא רק שהבנייה עוברת.

### בוצע
- **גרסה ניידי כקובץ יחיד.** סונן שהאריזה הקודמת (63MB עם תיקיית ספריות נלווית) הוחלפה
  בקובץ אחד (`PublishSingleFile` + `IncludeNativeLibrariesForSelfExtract`), כך שאין תיקייה
  ואין מה לחלץ. הגודל: 69MB.
- **מתקין MSI עם WiX.** נדרשו שלושה ניסיונות עד לבנייה נקייה:
  * **WiX 7** — נדחה: דורש הסכמה ל-Open Source Maintenance Fee (התחייבות כספית).
  * **WiX 5** (MIT) — נבחר. נדרש פרויקט בפורמט WiX (`FrameFit.wixproj`) כדי למשוך את
    תוסף ממשק המשתמש.
  * נדרש קידוד **1255** עבור מחרוזות עבריות, ומעבר למשתנה `PublishDir` במקום נתיב יחסי
    (ההפניה `..\publish\single\` נפתרה לתיקייה הלא נכונה).
- **בדיקת קבלה מלאה של המתקין:** התקנה שקטה (קוד יציאה 0), אימות הקובץ והקיצור בתפריט
  התחל, **הרצת הקובץ שהותקן במצב בדיקה עצמית — 10/10 עברו**, והסרה שקטה.
- **הבדיקה נשמרה כסקריפט במאגר:** `installer/test-install.ps1` — שבעה שלבים, מחזיר קוד
  יציאה שאינו אפס אם משהו נכשל, וניתן להריץ מחדש על כל מהדורה.
- **החלפת נכסי המהדורה `v0.1.0`:** ה-zip הוסר, ובמקומו המתקין, הגרסה הניידת, ושני
  קובצי `SHA-256`. רשומת השחרור נוסחה מחדש בעברית, ומציינת במפורש מה נבדק ומה טרם נבדק.
- **תיקון ה-workflow:** הוא נבנה מחדש כך שיבנה את הקובץ הבודד, יבנה את המתקין, יכתוב
  קובצי סכום בדיקה, ויעלה ארבעה נכסים. כמו כן נוסף `dotnet tool restore` לשחזור WiX.

### קבצים שהושפעו
- `installer/FrameFit.wxs`, `installer/FrameFit.wixproj`, `installer/license.rtf` (חדשים)
- `.config/dotnet-tools.json` (חדש — WiX 5 ככלי ברמת הפרויקט)
- `.github/workflows/build.yml`, `README.md`, `.gitignore`
- `docs/DECISIONS.md` (D10), `docs/TASKS.md`, `docs/CHANGELOG.md`, `docs/ARCHITECTURE.md`

### בדיקות
- בניית ה-MSI: 0 שגיאות, כל בדיקות ה-ICE עברו למעט `ICE61` ו-`ICE91` שהושתקו באופן ממוקד
  ומתועד (הסרת גרסה קודמת, והתקנה למשתמש בהתאמה).
- **בדיקת קבלה על המתקין** — ראו "בוצע": התקנה, קיצור דרך, הרצת הקובץ המותקן (10/10),
  והסרה שהחזירה את המערכת למצב נקי.
- אימות סכומי הבדיקה של שני הקבצים שפורסמו מול ה-`digest` שגיטהאב מדווחת.

### בעיות
1. **`msiexec` לא קיבל ארגומנטים דרך ה-shell של Git Bash** וחזר בשגיאה 1 בלי הסבר.
   הפתרון: מעבר ל-PowerShell עם נתיבים ב-ASCII, וקובץ סקריפט (`installer/test-install.ps1`).
   בבדיקה חוזרת התגלה סיבוך נוסף: `%TEMP%` מוחזר בפרופיל עם שם משתמש שאינו ASCII בצורת
   שם קצר (8.3), ואז `Copy-Item` ו-`Remove-Item` ב-PowerShell 5.1 מחזירים שגיאות-שווא.
   הוחלף לנתיב ASCII קבוע (`C:\Users\Public\FrameFitTest`).
2. **WiX 7 דורש הסכמה לתשלום** (Open Source Maintenance Fee). לא התקבלה בשם המשתמש —
   הוחלף ל-WiX 5.
3. **נתיב האריזה נפתר לתיקייה שגויה**, ובנוסף נדרש קידוד 1255 למחרוזות העבריות.
4. **קובץ `release-notes.md` נכנס בטעות לתיקיית ההפצה** ששימשה לבניית המתקין. הועבר
   מהתיקייה, ואומת שהמתקין נבנה מחדש בלעדיו.

### פתרון
1. סקריפט התקנה/הסרה ב-PowerShell, כדי שלא יהיה תלוי בהרחבת משתנים של ה-shell.
2. WiX 5 במקום 7 — אותה תוצאה, רישיון חופשי.
3. `<DefineConstants>PublishDir=...</DefineConstants>` בפרויקט, ו-`Codepage="1255"`.
4. הפרדה: תיקיית `publish/single/` משמשת **רק** לתוצר ההרצה.

### סטטוס
הושלם. המהדורה `v0.1.0` כוללת מעתה מתקין MSI וקובץ ניידי —
https://github.com/Lev-Good/FrameFit/releases/tag/v0.1.0

### מה טרם נבדק
- **שומר מסך-המלא** — עדיין לא הורץ מול דפדפן ב-F11, מצגת או נגן מדיה. זה הפער העיקרי.
- התקנה על מחשב **נקי** (ללא .NET) — נבדקה רק על מחשב הפיתוח, שבו מותקן ה-SDK.

---

## 2026-09-14 — זכויות יוצרים, מטא-דאטה של מוצר, והודעות צד-שלישי

### מטרה
לענות על שאלת המשתמש — האם הוגדרה שמירת זכויות על הפיתוח — ולסגור את מה שאפשר
לסגור בלי לשנות את כיוון הרישיון.

### בוצע
- **בירור מצב:** נמצא שרישיון MIT כבר קיים (`LICENSE`), מוצג באשף ההתקנה
  (`installer/license.rtf`), ומופנה מה-README. כלומר הזכויות מיוחסות מלכתחילה.
- **בירור משפטי** (מידע כללי, לא ייעוץ משפטי): זכות יוצרים בישראל נוצרת אוטומטית,
  בלי חובת רישום (חוק זכות יוצרים תשס"ח-2007; אמנת ברן). מנגד, MIT מעניקה לאחרים
  שימוש, שינוי והפצה — כלומר אינה כלי לשמירת בלעדיות.
- **תוספת מטא-דאטה** ל-`src/FrameFit.App/FrameFit.App.csproj`: `Product`, `Company`,
  `Copyright`, `Version=0.1.0`. הגרסה נקבעה כדי להתאים ל-`Version` של המתקין.
- **קובץ חדש:** `THIRD-PARTY-NOTICES.md` — מפרט את רכיבי הצד-שלישי שבהפצה
  (‎.NET 10 Runtime + WPF/WinForms — MIT; `WixToolset.UI.wixext` — MS-RL) ואת
  הודעת ה-MIT במלואה.
- **תיקון טענה שגויה:** `installer/FrameFit.wixproj` ו-`docs/DECISIONS.md` (D10) תיארו
  את רישיון WiX 5 כ-MIT. הרישיון הוא **MS-RL**. ההחלטה ב-D10 אינה משתנה, אך ההערה
  שגויה תוקנה בשני המקומות ונוספה השלכה שנשמטה (חובת שימור הודעת הזכויות).

### קבצים שהושפעו
- `src/FrameFit.App/FrameFit.App.csproj` (מטא-דאטה)
- `THIRD-PARTY-NOTICES.md` (חדש)
- `installer/FrameFit.wixproj`, `docs/DECISIONS.md` (תיקון טענת הרישיון; נוסף D11)
- `docs/TASKS.md`, `README.md`, `docs/CHANGELOG.md`

### בדיקות
- **בנייה:** `dotnet build FrameFit.slnx` — הצליחה, 0 אזהרות ו-0 שגיאות.
- **בדיקות יחידה:** `dotnet test tests/FrameFit.Core.Tests` — **52/52 עברו**.
- **אימות המטא-דאטה בפועל** (קריאה חזרה מהקובץ הבנוי, לא מהמקור):
  `CompanyName=Lev-Good`, `ProductName=FrameFit`, `LegalCopyright=Copyright (c) 2026 Lev-Good`,
  `FileVersion=0.1.0.0`, ו-`Comments` מכיל את התיאור בעברית ללא בעיית קידוד.
- **טרם נבדק:** בניית ה-MSI מחדש — WiX לא שוחזר בסביבה בסבב הזה, ולכן השינוי ב-
  `FrameFit.wixproj` הוא שינוי הערה בלבד וטרם נבנה. גם שילוב `THIRD-PARTY-NOTICES.md`
  בתוך ה-MSI לא בוצע.

### בעיות
1. **טענה שגויה חוזרת על רישיון WiX.** הופיעה בשני מקומות וייצרה הבנה מוטעית של
   המצב המשפטי. לא הייתה שגיאת בנייה — רק שגיאה תיעודית.
2. **חוסר התאמה בגרסה.** לקובץ ההרצה לא הוגדרה גרסה, ולכן נשא `1.0.0.0` בעוד המתקין
   הצהיר `0.1.0.0`. נמצא באגב, ותוקן.

### פתרון
1. אומת הרישיון מול מקורות (FireGiant, NuGet, אתר הפרויקט) ותוקן בשני המקומות, עם
   הפניה ל-`THIRD-PARTY-NOTICES.md`.
2. נוסף `<Version>0.1.0</Version>` ל-`FrameFit.App.csproj`.

### סטטוס
הושלם חלקית. מה שבוצע — בוצע ואומת. **מה שנותר פתוח מסומן במפורש:** שילוב קובץ
הודעות הצד-שלישי בתוך ה-MSI, ואימות בניית המתקין. שני הפריטים ב-`TASKS.md`.

---

## 2026-09-14 — תיקון מהשטח: אזור העבודה נגזל בידי סרגל המשימות

### מטרה
לטפל בשני הסימנים שדווחו אחרי התקנה והרצה על מסך אמיתי: סרגל המשימות נשאר בתחתית המסך
בתוך השוליים שהוגדרו כמוסתרים, וחלון במסך-מלא משאיר רצועה מתה בתחתית האזור הגלוי
בגובה סרגל המשימות.

### בוצע
- **אבחון מהנתונים בפועל, לא מהשערה:** נקראו `settings.json` ו-`framefit.log` של המשתמש
  (`%APPDATA%\FrameFit`), ונמדד ישירות מול Windows: `rcMonitor` = 1920×1080,
  `rcWork` = 1920×1032, וסרגל המשימות ב-`0,1032 1920×48`. השוליים שהוגדרו:
  L196 R213 T37 **B410** → אזור גלוי `196,37 1511×633`, אך אזור העבודה בפועל יצא
  קצר ב-48 פיקסלים בדיוק.
- **שורש הבעיה:** `ABM_QUERYPOS` מציב סרגל יישום *בצמוד* למה שכבר תפס את אותו צד, ולכן
  הסרגל שלנו נדחק מעל לסרגל המשימות, ושתי ההקצאות הצטברו במקום להתלכד. בנוסף, סרגל
  המשימות הוא חלון on-top של ה-Shell ולכן צויר מעל שכבת הכיסוי.
- **לוגיקה חדשה בליבה** (`Core/Geometry/WorkAreaCalculator.cs`): תכנון ההזמנה מול הקצאות
  קיימות (`ReservedInsets`, `WorkAreaPlan`), כולל חישוב "חוסר" — פיקסלים באזור הגלוי
  שלא ניתן להחזיר. **13 בדיקות יחידה חדשות**, בהן מקרה רגרסיה עם המספרים האמיתיים.
- **`TaskbarHost` חדש** (Platform): איתור `Shell_TrayWnd` / `Shell_SecondaryTrayWnd` של המסך
  המנוהל, הסתרה כל עוד ההגדרה פעילה, שמירה קצרה מפני החזרה עצמית של ה-Shell, והחזרה בכל
  מסלול יציאה. רשת ביטחון: קובץ סימון שמחזיר את הסרגל בעלייה הבאה אחרי הריגת התהליך.
- **`WorkAreaHost`** מודד את ההקצאות הקיימות (אחרי ההסתרה, עם השהיה קצרה להתייצבות),
  מזמין רק את ההפרש, ומחזיר את הסרגל ב-`Revert`.
- **אפשרות חדשה בפרופיל ובממשק:** "הסתרת סרגל המשימות של Windows במסך הזה" — ברירת מחדל
  פעילה, עם הסבר שהסרגל מוחזר ביציאה ובמקש המילוט.
- **אבחון:** אזור עבודה מצופה מול בפועל, סרגל המשימות (מיקום, מצב, כלילה בשוליים), ורצועה
  שנותרה תפוסה.
- **הבדיקה העצמית חוזקה** — היא לא תפסה את התקלה. נוספו: השוואה למידה מצופה, בדיקת סרגל
  המשימות, ומקרה שני של שוליים עמוקים (תרחיש המסגרת).
- **גרסה:** `0.1.0` → `0.1.1` בשלושת המקומות (`App.csproj`, `FrameFit.wxs`, `OutputName`)
  כדי שאפשר יהיה להתקין את הבנייה המתוקנת מעל הקודמת (MajorUpgrade). המהדורה טרם פורסמה.

### קבצים שהושפעו
- `src/FrameFit.Core/Geometry/WorkAreaCalculator.cs` (חדש), `src/FrameFit.Core/AppPaths.cs`
- `tests/FrameFit.Core.Tests/WorkAreaCalculatorTests.cs` (חדש)
- `src/FrameFit.Platform.Windows/WorkArea/TaskbarHost.cs` (חדש), `WorkAreaProbe.cs` (חדש),
  `WorkAreaHost.cs` (נכתב מחדש)
- `src/FrameFit.Platform.Windows/Interop/Native.cs`, `Diagnostics/DiagnosticsService.cs`
- `src/FrameFit.App/Services/FrameFitSession.cs`, `ViewModels/MainViewModel.cs`,
  `MainWindow.xaml`, `SelfTest.cs`, `FrameFit.App.csproj`
- `src/FrameFit.Core/Profiles/FrameFitProfile.cs`, `installer/FrameFit.wxs`,
  `installer/FrameFit.wixproj`
- `README.md`, `docs/SPEC.md`, `docs/ARCHITECTURE.md`, `docs/DECISIONS.md` (D12),
  `docs/TASKS.md`, `docs/CHANGELOG.md`

### בדיקות
- **בנייה:** `dotnet build FrameFit.slnx` — 0 אזהרות, 0 שגיאות.
- **בדיקות יחידה:** `dotnet test tests/FrameFit.Core.Tests` — **65/65 עברו** (52 + 13).
- **בדיקה עצמית על המחשב:** `FrameFit.exe --self-test` — **16/16 עברו**, ובהן:
  * שוליים עמוקים (L196 R213 T37 B410): אזור העבודה בפועל = `196,37 1511×633` —
    **בדיוק** האזור הגלוי. לפני התיקון היה `1511×585`.
  * שוליים רדודים: אזור העבודה תואם את המצופה, עם דיווח מפורש על 32 הפיקסלים
    שסרגל המשימות תופס (כמצופה כשהשוליים קטנים מגובהו).
  * סרגל המשימות: הוסתר בזמן ההגדרה והוחזר בשחזור — בשני המצבים.
- **קריאה ישירה ממערכת ההפעלה** לפני/אחרי: `rcWork` חזר ל-`0,0 1920×1032` והסרגל נראה שוב.

### בעיות
1. **הבדיקה העצמית הייתה חלשה מדי** ולכן סימנה "עבר" גם כשהתקלה הייתה קיימת: היא בדקה
   **הכלה** של אזור העבודה בתוך האזור הגלוי, ומצב כזה מתקיים גם כשחסרים 48 פיקסלים.
   הלקח: דרישה של "בתוך" אינה מספיקה — נדרש **שוויון** מול המידה המצופה.
2. **הסתרת הסרגל אינה משחררת את ההקצאה שלו.** נמדד בפועל: `rcWork` נשאר `1920×1032`
   בזמן שהסרגל מוסתר. לכן שני חלקי התיקון תלויים זה בזה.
3. **התנהגות לא מוגדרת שנמנעה:** הסתמכות על סרגל יישום שחופף לסרגל המשימות נפסלה מראש,
   כי אינה מתועדת ואינה ניתנת לבדיקה; נבחר קיזוז **מדוד** במקום זאת.

### פתרון
הוגדרה לוגיקת תכנון הנבדקת ביחידה, וממנה נגזרים הרישום, ההודעות למשתמש והאבחון. המקרים
שהתגלו בשטח הפכו למקרי בדיקה קבועים — בבדיקות היחידה ובבדיקה העצמית.

### סטטוס
הושלם ואומת. **מה שנותר פתוח ומסומן ב-`TASKS.md`:** פרסום מהדורת 0.1.1, והמגבלה כשהשוליים
קטנים מגובה סרגל המשימות (מדווחת כאזהרה — אין פתרון טכני ידוע).

---

## 2026-09-14 — בנייה, התקנה ואימות של מהדורת 0.1.1

### מטרה
לבנות את מתקין 0.1.1, להתקין אותו מעל 0.1.0 שהיה מותקן, ולאמת שהתיקון קיים ופועל
**בקובץ המותקן** — לא רק בקוד המקור.

### בוצע
- `dotnet tool restore` — כלי WiX 5.0.2 שוחזר (בסבב הקודם הוא לא היה זמין).
- `dotnet publish` self-contained לקובץ בודד → `publish/single/FrameFit.exe` (66.1MB),
  `FileVersion=0.1.1.0`, `Company=Lev-Good`, העברית נשמרת.
- `dotnet build installer/FrameFit.wixproj -c Release` → `FrameFit-v0.1.1-x64.msi` (63.1MB),
  0 אזהרות ו-0 שגיאות.
- ההתקנה בוצעה מנתיב ASCII (`C:\Users\Public\FrameFitInstall`) ולא מנתיב המאגר — לנתיב
  עם תווים עבריים יש בעיה מוכרת ב-msiexec. `msiexec /i … /qn /norestart /l*v` → קוד 0.
- הורץ `--self-test` של **הקובץ המותקן** (ולא של הקובץ הבנוי ב-`bin`).

### קבצים שהושפעו
- אין שינויי קוד בסבב הזה. עודכנו: `docs/TASKS.md`, `docs/CHANGELOG.md`,
  `docs/AGENT_LOG.md`, `README.md`.

### בדיקות
- **התקנה:** קוד יציאה 0. בלוג: `FindRelatedProducts` → `RemoveExistingProducts`, כלומר
  המוצר הקודם (0.1.0) הוסר במסגרת MajorUpgrade.
- **הקובץ המותקן:** `%LOCALAPPDATA%\FrameFit\FrameFit.exe` עודכן ל-`FileVersion=0.1.1.0`
  (לפני: `1.0.0.0` — הבנייה הישנה, שקדמה גם לתיקון המטא-דאטה). קיצור הדרך בתפריט התחל
  מצביע עליו.
- **רשומת הסרה:** `FrameFit 0.1.1.0` תחת
  `HKLM\Software\Microsoft\Windows\CurrentVersion\Uninstall\{425BC611-235A-458A-BB59-D30BB293C1AE}`.
- **בדיקה עצמית של הקובץ המותקן: 16/16 עברו.** המקרה הרלוונטי (L196 R213 T37 B410):
  אזור העבודה בפועל = `196,37 1511×633` — בדיוק האזור הגלוי (לפני התיקון: `585`).
  סרגל המשימות הוסתר בזמן ההגדרה והוחזר בשחזור.
- **מצב המערכת אחרי הבדיקה:** אזור העבודה חזר ל-`1920×1032`, הסרגל גלוי, אין
  `taskbar-hidden.marker`, ו-`settings.json` (עם הפרופיל L196 R213 T37 B410) לא נפגע.

### בעיות
1. **רשומת "הוספה או הסרה של תוכניות" נכתבה ל-`HKLM` ולא ל-`HKCU`.** ההתקנה בוצעה בהקשר
   ש**אינו** מוגבר (נבדק במפורש: `IsInRole(Administrator) = False`), והקבצים הותקנו
   ב-`%LOCALAPPDATA%` כמצופה — אך הרשומה נוצרה תחת HKLM (הלוג מציג `RegisterProduct`
   ו-`PublishProduct`). **המשמעות הטובה:** הרשומה קיימת ונראית למשתמש, כך שההסרה מהממשק
   עובדת. **מה שלא הובהר:** אם הצהרת ה-`Scope` של החבילה אכן מובילה להתנהגות per-user,
   ואיך זה מתנהג בהתקנה בלחיצה כפולה (בלי `/qn`). נרשם כמשימה פתוחה ב-`TASKS.md`, בלי
   מסקנה שלא הוכחה.
2. **הבדיקה העצמית משנה לזמן קצר את אזור העבודה** (שני מצבי שוליים, כ-4 שניות) — התנהגות
   מוכרת ומכוונת של הבדיקה, והמצב מוחזר בסיומה. במכוון **לא** בוצעה החלה מלאה של הפרופיל
   (שוליים שחורים, עכבר חסום, סרגל מוסתר), כדי לא להשאיר את המחשב במצב מוחל בזמן העבודה.

### סטטוס
הושלם. המתקין של 0.1.1 בנוי, מותקן ומאומת על המחשב הזה. **מה שנותר:** פרסום המהדורה
ב-GitHub (תגית + Release), ואימות התקנה בהרצה לא-מוגברת.


## 2026-09-14 — עיגון סרגל המשימות בתוך האזור הגלוי (0.1.2)

### מטרה
המשתמש דיווח שהתקין את `0.1.1` והפעיל, וסרגל המשימות **נשאר מוסתר** ולא עבר אל תוך
האזור הגלוי. היעד: הסרגל יישאר גלוי ושמיש בתוך האזור הגלוי, ומצד שני לא יגזול פיקסלים
מהתוכן (חלון ממוקם לא ייעלם מתחתיו).

### בוצע
- **ברירת מחדל חדשה:** `TaskbarMode.MoveIntoVisibleArea` — הסרגל מועבר לתחתית האזור הגלוי
  ומעוגן שם. ההסתרה (`HideInHiddenArea`) ואי-ההתערבות נשארו כאפשרויות בממשק (רדיו).
- **רצועת העיגון נגרעת מאזור העבודה (FR-5.3):** `WorkAreaCalculator.Plan` מקבל `bottomAnchor`
  ומחזיר `WorkAreaPlan.BottomAnchor`; `WorkAreaHost.TryApply` מוסר את גובה הסרגל שנעגן.
  אותה רצועה **אינה** נספרת כ"חוסר" (Shortfall) ואינה מדווחת כאזהרה.
- **אי-מאבק ב-Shell במסך מלא:** `TaskbarHost.EnsurePosition` אינו מחזיר סרגל שהוסתר ע"י
  מישהו אחר (ה-Shell מסתיר אותו בעצמו כשחלון נכנס למסך מלא).
- אבחון: `WorkAreaStatus.TaskbarAnchor` והצגת רצועת הסרגל המעוגן בנפרד מאזור העבודה המצופה.
- ממשק: נוסח אפשרויות סרגל המשימות עודכן; אזהרת "הסמל בסרגל המערכת" צומצמה למצב ההסתרה בלבד.
- גרסה: `0.1.1` → `0.1.2` בשלושת המקומות, כדי שאפשר יהיה להתקין מעל `0.1.1` (MajorUpgrade).

### קבצים שהושפעו
- `src/FrameFit.Core/Geometry/WorkAreaCalculator.cs` (‏`bottomAnchor`, `BottomAnchor`)
- `src/FrameFit.Core/Profiles/FrameFitProfile.cs` (‏`TaskbarMode` וברירת המחדל)
- `src/FrameFit.Platform.Windows/WorkArea/TaskbarHost.cs` (‏`MoveInto`, שמירת גובה ב-Shell)
- `src/FrameFit.Platform.Windows/WorkArea/WorkAreaHost.cs` (העברת ה-anchor לתוכנית, סיכום)
- `src/FrameFit.App/SelfTest.cs` (בדיקת "נשאר גלוי"), `MainWindow.xaml`, `DiagnosticsService.cs`,
  `FrameFitSession.cs`, `FrameFit.App.csproj`, `installer/FrameFit.wxs`, `installer/FrameFit.wixproj`
- בדיקות: `tests/FrameFit.Core.Tests/WorkAreaCalculatorTests.cs`, `ProfileTests.cs`
- תיעוד: `SPEC.md` (FR-5.2, FR-5.3, מגבלה 5-6, טבלת Q), `ARCHITECTURE.md` (3.2, 3.2.1),
  `DECISIONS.md` (D13 + אינדקס D12/D13), `TASKS.md`, `CHANGELOG.md`, `README.md`, ויומן זה

### בדיקות
- בנייה: 0 אזהרות, 0 שגיאות.
- **70/70 בדיקות יחידה** (נוספו 4 מקרים לרצועת העיגון ומקרה רגרסיה לטעינת הגדרות מגרסה קודמת).
- **16/16 בדיקות עצמיות** על המחשב הזה, מול סרגל משימות אמיתי (1920×1080, סרגל 48):
  * שוליים רדודים (L40 R60 T24 B16): אזור גלוי `40,24 1820×1040`; הסרגל עוגן
    ב-`40,1016 1820×48`; אזור עבודה בפועל `40,24 1820×992` = **היעד המדויק**.
  * שוליים עמוקים (L196 R213 T37 B410): אזור גלוי `196,37 1511×633`; הסרגל עוגן
    ב-`196,622 1511×48`; אזור עבודה בפועל `196,37 1511×585` = **היעד המדויק**;
    התוכן והסרגל מכסים יחד את כל האזור הגלוי.
  * הסרגל **נשאר גלוי** בזמן העיגון והוחזר למקומו (0,1032 1920×48) בשחזור; אזור העבודה חזר
    ל-1920×1032; לא נותר קובץ סימון יתום.
- הגדרות המשתמש נטענו בלי איפוס, כולל השדה הישן `HideTaskbar` (אומת בבדיקת יחידה).

### בעיות
1. **ההסתרה כברירת מחדל (`0.1.1`) לא התאימה למשתמש** — הסרגל נעלם ואינו שמיש.
2. **העברת הסרגל לבדה אינה מספיקה.** אחרי המעבר לעיגון אומת שהסרגל עוגן בתוך האזור הגלוי,
   אך אזור העבודה נשאר בגובה האזור הגלוי כולו (`1511×633`), כלומר התוכן נמתח עד תחתיתו
   וסרגל on-top כיסה 48 פיקסלים מכל חלון ממוקם. הבדיקה העצמית תפסה זאת (בדיקת ה"ריצוף"
   נכשלה) — כלומר הבדיקה החדשה מהסבב הקודם עבדה.

### פתרון
גובה הסרגל המעוגן נגרע מאזור העבודה (`bottomAnchor`), והרצועה מדווחת כחלק מכוון ולא כגזילה.
רצועת הסרגל שלנו ממוקמת מעל ההקצאה הקיימת של Windows, ולכן אזור העבודה מסתיים בקצהו העליון
של הסרגל — בדיוק כמתוכנן, ובשני מצבי השוליים.

### סטטוס
הושלם ואומת בבדיקה עצמית על המחשב הזה. **מה שנותר פתוח ומסומן ב-`TASKS.md`:** המתקין של
`0.1.2` טרם נבנה והבנייה המתוקנת טרם הותקנה מעל `0.1.1`, ולכן ההתנהגות טרם נצפתה בממשק
המשתמש (רק בבדיקה העצמית); פרסום המהדורה טרם בוצע; ומגבלת "שוליים קטנים מסרגל המשימות"
נותרה במסלול ההסתרה בלבד.

## 2026-09-14 — בנייה, התקנה ואימות של 0.1.2 (מעל 0.1.1)

### מטרה
לבנות את מתקין `0.1.2`, להתקין אותו מעל `0.1.1` שהיה מותקן, ולאמת שהסרגל נעגן באזור הגלוי
ושהתוכן מסתיים מעליו.

### בוצע
- `dotnet tool restore` → WiX 5.0.2.
- `dotnet publish -c Release -r win-x64 --self-contained -p:PublishSingleFile=true` →
  `publish/single/FrameFit.exe` (66.1MB, `FileVersion=0.1.2.0`, `Company=Lev-Good`,
  `Copyright=Copyright (c) 2026 Lev-Good`).
- `dotnet build installer/FrameFit.wixproj -c Release` → `FrameFit-v0.1.2-x64.msi` (63.1MB,
  0 אזהרות, 0 שגיאות).
- התקנה מנתיב ASCII (`C:\Users\Public\FrameFitInstall`), `msiexec /i … /qn /norestart /l*v`
  → **קוד יציאה 0**. בלוג: `WIX_UPGRADE_DETECTED={425BC611-…}` (המוצר של 0.1.1) ואחריו
  `RemoveExistingProducts`.
- **תקלה שאותרה במהלך האימות ותוקנה** (ראו "בעיות"): מירוץ בשחזור סרגל המשימות. אחרי התיקון
  בוצע מסלול נקי — הסרה (`msiexec /x`, קוד 0) והתקנה של ה-MSI המתוקן.

### קבצים שהושפעו
- `src/FrameFit.Platform.Windows/WorkArea/TaskbarHost.cs` — `Restore()` מנקה את המצב תחת
  המנעול לפני הפעולות, מדווח כשל החזרה כאזהרה, ו-`ReadCurrentRect()` לקריאה מהמערכת.
- `src/FrameFit.App/SelfTest.cs` — אימות השחזור נקרא מהמערכת ולא מהמצב השמור.
- תיעוד: `TASKS.md`, `CHANGELOG.md`, `README.md`, ויומן זה.

### בדיקות
- בנייה: 0 אזהרות, 0 שגיאות; **70/70 בדיקות יחידה**.
- **הבדיקה העצמית של הקובץ המותקן: 16/16 עברו.**
  * שוליים רדודים (L40 R60 T24 B16): אזור גלוי `40,24 1820×1040`; הסרגל עוגן
    ב-`40,1016 1820×48`; אזור עבודה בפועל `40,24 1820×992` = **היעד המדויק**.
  * שוליים עמוקים (L196 R213 T37 B410): אזור גלוי `196,37 1511×633`; הסרגל עוגן
    ב-`196,622 1511×48`; אזור עבודה בפועל `196,37 1511×585` = `196,37 1511×633` פחות 48 —
    **התוכן מסתיים בקצהו העליון של הסרגל ואין חור ואין חפיפה**.
  * הסרגל **נשאר גלוי** בזמן העיגון, והוחזר ל-`0,1032 1920×48` בשחזור.
  * בשני המקרים: אזור העבודה חזר ל-`1920×1032`, הסרגל גלוי במקומו הטבעי, ואין קובץ סימון.
- מתקין: ARP מציג `FrameFit 0.1.2.0`; הקובץ המותקן
  `%LOCALAPPDATA%\FrameFit\FrameFit.exe` = `0.1.2.0`; קיצור הדרך מצביע עליו; הגדרות המשתמש
  נשמרו. הסרה: הקבצים והקיצור הוסרו, וקובץ ההגדרות נשאר.

### בעיות
ההתקנה הראשונה של `0.1.2` סיימה את הבדיקה העצמית ב-**15/16**: במקרה של השוליים העמוקים
הסרגל נרשם כ\"הוחזר אל `196,622 1511×48`\" — כלומר נשאר במקום המעוגן ולא חזר לשורת התחתית.
הבדיקה של אזור העבודה דווחה כעוברת, ולכן היה נדרש לבדוק את מלבן הסרגל **מהמערכת** כדי לראות
את הפער. בדיקה חוזרת של מצב המחשב מיד לאחר מכן הראתה שהסרגל דווקא חזר למקומו — מה שהצביע על
מירוץ ולא על כשל קבוע.

### פתרון
`Restore()` פינה את המצב הפנימי (`_hiddenByUs`/`_movedByUs`/`_moveTarget`) **אחרי** החזרת
הסרגל. פעימת השומר שרצה ברקע (כל שנייה) הספיקה לראות \"עדיין מועבר\", לקרוא את היעד המעוגן
ולהחזיר את הסרגל אליו מיד לאחר שהשחזור החזיר אותו למטה; הוצאת התהליך אחר כך (Dispose →
StopWatchdog → Restore נוסף) חזירה אותו סופית למקום הטבעי — ולכן המצב שנמדד מהמערכת היה תקין
בעוד הבדיקה דיווחה על כשל. המצב מנוקה מעתה תחת המנעול **לפני** הפעולות, כך שפעימה מקבילה
רואה \"לא מטופל\" ואינה נוגעת בסרגל; בנוסף, כשל החזרה מדווח כאזהרה עם המיקום שנקרא חזרה,
והבדיקה העצמית קוראת את מלבן הסרגל ממערכת ההפעלה (`ReadCurrentRect`) ולא מהמצב השמור.

### סטטוס
הושלם. `0.1.2` בנוי, מותקן ומאומת על המחשב הזה. **מה שנותר פתוח:** פרסום המהדורה (טרם בוצע),
הרצת התוכנה המותקנת מול הפרופיל האמיתי (החלה חיה — בכוונה לא בוצעה), ונושא מקום רשומת ה-ARP
לחבילה מוצהרת `perUser`.

## 2026-09-14 — הפעלה חיה בשטח: אי אפשר להזיז את סרגל המשימות (D14, גרסה 0.1.3)

### מטרה
המשתמש התקין את `0.1.2`, הפעיל אותה, ודיווח שסרגל המשימות "עדיין במקומו". הוא אישר החלה חיה
של הפרופיל כדי שאמדוד את המצב בעצמי ואדע את האמת — במקום להסתמך על הבדיקה העצמית.

### בוצע
- קריאת הלוג של ההפעלה החיה: ההחלה בוצעה עם השוליים האמיתיים (L196 R213 T37 B175), אזור
  העבודה התקבל `196,37 1511×820`, והסרגל נרשם כ"עוגן בתחתית האזור הגלוי (196,857 1511×48)".
- **מדידה ישירה של מלבן הסרגל מהמערכת:** `0,1032 1920×48` — במקומו הדבוק, לא ביעד.
- דגימה חוזרת ב-25 הרץ במשך 12 שניות: **300 מתוך 300** דגימות — הסרגל לא שהה ביעד אף פעם.
- ניסוי בקרה — הזזה למקום שלישי (`SetWindowPos` בלבד) → הסרגל חזר תוך פחות מ-300 מ״ש.
- ניסוי שני — בדיוק הפעולה של התוכנה (`ABM_SETPOS` + `SetWindowPos`) → **150 מתוך 150** דגימות
  הראו חזרה.
- ניסוי שלישי — 200 הזזות רצופות כל 20 מ״ש, עם קריאה חוזרת מיידית → **200 מתוך 200** חזרו
  כבר בקריאה שאחרי ההזזה.
- בדיקת הגדרות: הסתרה-אוטומטית כבויה, אין מדיניות, הקצה הדבוק הוא תחתית — התנהגות ה-Shell.
- **תיקון הקוד:** `TaskbarHost.MoveInto` מאמת את המיקום שקוראים חזרה מהמערכת (חלון של 350
  מ״ש) במקום להסתמך על הצלחת `SetWindowPos`; בכשל — הסרגל מוחזר למקומו, המצב הפנימי מנוקה,
  ואין גזילת רצועה. ברירת המחדל של `TaskbarMode` הועברה ל-`LeaveInPlace`.
- **תיקון הבדיקה העצמית:** היא מודדת את מלבן הסרגל **מהמערכת** בעת ההחלה, ומקבלת רק שני
  מצבים עקביים — מעוגן ומרוצף מול התוכן, או דבוק בקצה עם אזור עבודה מלא. נוסף גם מסלול
  כשל לרצועה מוקצית שאין בה סרגל.
- סיום ההפעלה החיה: מקש המילוט (`Ctrl+Alt+Shift+F12`) שוחזר במלואו (הלוג: "כל ההגדרות
  שוחזרו למצב Windows נקי", אזור עבודה `1920×1032`), ואחריו נסגר התהליך לצורך ההתקנה.
- בנייה והתקנה: `FrameFit-v0.1.3-x64.msi` מעל `0.1.2` (קוד יציאה 0).

### קבצים שהושפעו
- `src/FrameFit.Platform.Windows/WorkArea/TaskbarHost.cs` — אימות מיקום, `ReadCurrentRect`,
  דיווח על החסימה.
- `src/FrameFit.Core/Profiles/FrameFitProfile.cs` — ברירת מחדל `LeaveInPlace` ותיעוד המגבלה.
- `src/FrameFit.App/SelfTest.cs` — מדידת הסרגל בפועל ושני המצבים העקביים.
- `src/FrameFit.App/MainWindow.xaml`, `Services/FrameFitSession.cs` — נוסח ואי-הצגת הצלחה.
- `FrameFit.App.csproj`, `installer/FrameFit.wxs`, `installer/FrameFit.wixproj`,
  `installer/test-install.ps1` — גרסה `0.1.3`.
- תיעוד: `DECISIONS.md` (D14 + סימון D13 כמבוטל), `SPEC.md`, `ARCHITECTURE.md`, `TASKS.md`,
  `CHANGELOG.md`, `README.md`, ויומן זה.

### בדיקות
- בנייה: 0 אזהרות, 0 שגיאות; **70/70 בדיקות יחידה** (בדיקת הרגרסיה לטעינת הגדרות עודכנה
  לברירת המחדל החדשה).
- **הבדיקה העצמית של הקובץ המותקן (0.1.3): 16/16.**
  * שוליים עמוקים: האזור הגלוי `196,37 1511×633`, ואזור העבודה **בפועל = 196,37 1511×633**
    (האזור הגלוי במדויק) — כלומר אין יותר רצועה מתה.
  * הסרגל: יעד `196,622 1511×48`, בפועל `0,1032 1920×48` — זוהה כמי שהוחזר, ולא נגזעה רצועה.
  * שוליים רדודים: האזור הגלוי `40,24 1820×1040`, אזור עבודה בפועל `40,24 1820×1008`
    (החוסר הידוע של 32 פיקסלים — השוליים קטנים מהסרגל).
  * בשני המקרים: אזור העבודה חזר ל-`1920×1032`, הסרגל גלוי במקומו, ואין קובץ סימון.
- מצב המחשב בסיום: הסרגל `0,1032 1920×48` גלוי, אזור העבודה `1920×1032`, FrameFit לא רץ,
  ואין קובץ סימון.

### בעיות
1. **העברת סרגל המשימות אינה אפשרית ב-Windows 11** (נמדד, ראו לעיל) — כך ששתי ההחלטות
   הקודמות (הסתרה ב-`0.1.1`, עיגון ב-`0.1.2`) טיפלו בתופעה שלא ניתנת לפתרון בדרך הזו.
2. **הבדיקה העצמית נתנה אישור שגוי.** היא השוותה את הסרגל לתוכנית (`MoveTarget`) ולא למצב
   בפועל, ולכן דיווחה "עיגון: כן" בעוד הסרגל ישב במקומו. זו הסיבה שהצהרתי קודם שהתיקון
   אומת — ההצהרה הייתה מבוססת על בדיקה פגומה. הפער נחשף רק במדידה ישירה מהמערכת.

### פתרון
הוחלפה ההנחה במדידה: הקוד מאמת את המיקום בפועל, הבדיקה מודדת מהמערכת, והתיעוד מצהיר את
המגבלה במקום להציג יכולת שלא קיימת. הרצועה המתה נעלמה — אזור העבודה שווה לאזור הגלוי.

### סטטוס
הושלם ואומת. **מה שנותר פתוח:** ההחלטה אם לבנות סרגל חלופי של FrameFit בתוך האזור הגלוי
(הדרך היחידה לסרגל שמיש שם), פרסום המהדורה, ונושא מקום רשומת ה-ARP.

## 2026-09-14 — החלטת המשתמש: הסרגל נשאר במקומו, בלי סרגל (0.1.4)

### מטרה
לסגור את שאלת סרגל המשימות אחרי שנמדד שאי אפשר להזיזו: לבחור כיוון, ליישם אותו, ולהתקין.

### בוצע
- **החלטת המשתמש:** "השאר אותו במקומו, בלי סרגל" — לא ייבנה סרגל חלופי של FrameFit, ולא
  מתערבים בסרגל של Windows. במקביל ביקש לעדכן את ברירת המחדל בפרופיל השמור.
- **הוסרה מהממשק האפשרות "ניסיון לעגן את הסרגל"** (נשארו "יישאר במקומו" ו"יוסתר", עם הסבר
  מפורש ש-Windows אינו מתיר את ההעברה). תכונת ה-ViewModel `TaskbarMove` הוסרה.
- **הגירת פרופיל:** `NormalizeTaskbarMode` — פרופיל שמור עם המצב שהוסר נטען כ"יישאר במקומו",
  בלי לאפס את יתר ההגדרות.
- **הפרופיל של המשתמש עודכן:** `TaskbarMode 0 → 2`, והשדה הישן `HideTaskbar` הוסר
  (`settings.json.bak` נשמר כגיבוי לפני השינוי). השוליים נשמרו כלשהם: L196 R213 T37 B175.
- גרסה `0.1.3` → `0.1.4` בארבעה מקומות; המתקין נבנה והותקן מעל `0.1.3` (קוד יציאה 0).

### קבצים שהושפעו
- `src/FrameFit.App/ViewModels/MainViewModel.cs` — הסרת `TaskbarMove` והוספת ההגירה.
- `src/FrameFit.App/MainWindow.xaml` — הסרת כפתור הבחירה וניסוח ההסבר.
- `FrameFit.App.csproj`, `installer/FrameFit.wxs`, `installer/FrameFit.wixproj`,
  `installer/test-install.ps1` — גרסה `0.1.4`.
- תיעוד: `DECISIONS.md` (החלטת המשתמש ב-D14), `CHANGELOG.md`, `TASKS.md`, `README.md`, ויומן זה.
- מחוץ למאגר: `%APPDATA%\FrameFit\settings.json` (+ `.bak`).

### בדיקות
- בנייה: 0 אזהרות, 0 שגיאות; **70/70 בדיקות יחידה**.
- **הבדיקה העצמית של הקובץ המותקן (0.1.4): 16/16.**
  * שוליים עמוקים (התרחיש של המשתמש): האזור הגלוי `196,37 1511×633`, ואזור העבודה בפועל
    `196,37 1511×633` — **האזור הגלוי במדויק**, בלי רצועה מתה ובלי גזילה.
  * הסרגל מדווח כמו שהוא: יעד לא רלוונטי, בפועל `0,1032 1920×48` (במקומו הדבוק).
  * השוליים הרדודים: החוסר הידוע של 32 פיקסלים (השוליים קטנים מהסרגל) מדווח במפורש.
  * אזור העבודה חזר ל-`1920×1032`, הסרגל גלוי, ואין קובץ סימון.
- מתקין: ARP מציג `FrameFit 0.1.4.0`, הקובץ המותקן `0.1.4.0`, קיצור הדרך מצביע עליו,
  והפרופיל נשמר (`TaskbarMode=2`, שוליים L196 R213 T37 B175).
- מצב המחשב בסיום: אזור עבודה `1920×1032`, הסרגל במקומו וגלוי, FrameFit לא רץ.

### בעיות
אין חדשות. הסיכון היחיד שנפתר: פרופיל שמור עם המצב שהוסר — טופל בהגירה אוטומטית, ולא
באמצעות איפוס הגדרות.

### סטטוס
הושלם ואומת. **מה שנותר פתוח:** פרסום המהדורה (טרם בוצע), ונושא מקום רשומת ה-ARP לחבילה
מוצהרת `perUser`.
